// props
type SectionProps = {
  level?: number,
  children: ReactNode,
  isFancy?: boolean
}

type HeadingProps = {
//   level: number,
  children: ReactNode
}

type PostProps = {
  title: string,
  body: ReactNode
}
